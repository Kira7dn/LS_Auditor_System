# ESG Source Inbox

This folder holds related source documents that have not yet been assigned to a dedicated source collection or ingested into the graph.

Before ingestion, move each document into `Projects/ESG/sources/<source_id>/`, create `manifest.source.json`, extract Markdown into `Projects/ESG/kb/<source_id>/`, then import/evaluate that collection.
